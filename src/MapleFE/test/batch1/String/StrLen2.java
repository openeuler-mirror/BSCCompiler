// Display the length of a given string
public class StrLen2 {
  public static void main(String[] args) {
    String x = "Failures are stepping stones to success.";
  
    int x_len = x.length();

    System.out.println("The length of \"" + x + "\"" + " is " + x_len);
  }
}
