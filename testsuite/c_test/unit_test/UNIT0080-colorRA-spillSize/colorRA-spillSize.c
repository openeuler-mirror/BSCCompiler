#include <stdio.h>
long long a;
int b, d, f;
short e, g;
_Bool i[][23][23][10][21];
unsigned short aa[][23][23][10][21];
unsigned short j[][15][21];
unsigned char k[][15][16];
long long l[][15][16][22];
int m[];
char n[];
short o[][5][0];
void fn1(long long *a, int c) { *a = c; }
void p(int, long long, unsigned short, _Bool, int, unsigned long long,
       signed char, unsigned, short, unsigned char, _Bool,
       _Bool[][23][23][10][21], unsigned short[][23][23][10][21],
       unsigned short[][15][21], unsigned char[][15][16],
       long long[][15][16][22]);
int main() {
  p(5, b, 80, 1, 64392445, 725817855635, 5, 70259, 2, 143, 0, i, aa, j, k, l);
  fn1(&a, f);
  printf("%llu\n", a);
}
#define ab(q, r) q
#define ac(q, r) q < r ? q : 0
void p(int s, long long b, unsigned short u, _Bool t, int w,
       unsigned long long v, signed char y, unsigned x, short ad,
       unsigned char ae, _Bool af, _Bool i[][23][23][10][21],
       unsigned short aa[][23][23][10][21], unsigned short j[][15][21],
       unsigned char k[][15][16], long long l[][15][16][22]) {
  d = d ? (long)ad : 0;
  for (long ag = ae - 143LL; ag; ag += 3)
    for (char ah = 0; ah < (char)b; ah += 3)
      for (char ai = 0; ai < ab((long)(0 ? 0 : s), ); ai += 9) {
        for (; g;)
          for (long aj = 0; aj < 037045; aj += 4)
            m[g] = aa[ag][ah][ai][g][aj];
        n[ag + ah + ai] = 7 ? ad : 0;
      }
  for (; ac(e, ae);)
    ;
  for (int ak = 0; ak < 7; ak += 2)
    for (char al = 0; al < 5; al += 3) {
      f = u;
      for (char am = 0; am < s; am += 4)
        for (long an = 0; an < 2; an += 4)
          for (short ao = 0; ao < 3; ao += 2)
            for (short ap = (v ?: b) + w; ap < 0; ap += e)
              for (long aq = 0; aq < t + 3ULL; aq += 2)
                o[ak][al][2] = (w ?: x) + (ac(af, y));
    }
}

